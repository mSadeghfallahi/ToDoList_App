# Database Configuration
# These values should match your PostgreSQL database setup
# If using docker-compose, these values match the default postgres service configuration

# Database username
DB_USER=todo_user

# Database password
DB_PASSWORD=todo_password

# Database host (use 'localhost' for local development, or your database server address)
DB_HOST=localhost

# Database port (default PostgreSQL port is 5432)
DB_PORT=5432

# Database name
DB_NAME=todo_db

# Application Configuration

# Maximum number of projects allowed (default: 10)
MAX_NUMBER_OF_PROJECTS=10

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Update the values according to your database configuration
# 3. Never commit .env to version control (it's already in .gitignore)
